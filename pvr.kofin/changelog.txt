v0.10.0
- Starting a recording no longer freezes the Kodi UI for several seconds: the timer is now created on a background thread instead of blocking the interface while the Jellyfin requests complete
- Recordings, home-screen widgets, and the EPG "Play recording" menu entry now refresh as soon as a recording starts or stops, instead of waiting up to 60 seconds for the next background poll
- Stopping a recording no longer briefly shows the deleted timer and still-in-progress recording: the UI now refreshes only after the change has been applied on the server and reloaded

v0.9.3
- Play in-progress recordings from the beginning instead of starting roughly 30 seconds in (the still-transcoding stream was being treated as live, so playback began near the playlist head rather than at the start)

v0.9.2
- Fix a potential crash from a data race on the Jellyfin channel/EPG lookup maps when a background channel/EPG reload coincided with browsing the guide or starting playback (map access is now synchronised)
- Clamp the channel update interval to a minimum of 1 hour so a 0 value can't trigger a continuous reload loop

v0.9.1
- Fix catchup playback for TS-based sources (Xtream Codes, Flussonic mpegts, shift/append on a .ts stream): MIME and manifest type are now derived from the resolved catchup URL instead of always being labelled HLS, so inputstream.ffmpegdirect no longer fails trying to parse raw MPEG-TS as a playlist

v0.9.0
- Add "Allowed HDR types" setting: choose which HDR/Dolby Vision formats pass through to your display; unselected formats are tone-mapped to SDR by the server (HEVC, AV1, VP9)
- Add "Maximum resolution" setting: cap playback resolution; higher-resolution sources are downscaled by the server
- TS transcoding profile leads with HEVC when AV1 is the preferred codec, so a forced transcode targets the more efficient codec

v0.8.0
- Dual TranscodingProfiles: AV1 streams use an fMP4 container and all other codecs use TS, replacing the transcoding-URL container-switch hack — the server selects the container by codec-copy compatibility
- Jellyfin v12 compatibility: use the non-deprecated Authorization header and ApiKey query parameter (instead of X-Emby-Authorization / api_key)
- Show Jellyfin server disk space in Kodi (recordings storage free/used)
- Preferred video codec setting clarified: AV1 is only used when the source codec is also AV1
- Internal: removed dead code inherited from pvr.iptvsimple; moved Jellyfin login/auth into a dedicated JellyfinAuth module

v0.7.2
- Fix AV1 fMP4 container switch not triggering when server reports DirectPlayError transcode reason

v0.7.1
- Add "Force direct play" global setting and kofin-force-direct-play per-channel KofinProp — sends wildcard DirectPlayProfile so the server proxies streams without transcoding or remuxing
- Force direct play hides all other transcoding settings when active
- Does not apply to in-progress recordings (always HLS remux)

v0.7.0
- Unified TranscodingProfile: single profile with preferred codec first, then all allowed codecs — server codec-copies when it can, transcodes to preferred when it must
- Add "Force transcoding" global setting — caps bitrate at source to force re-encode to preferred codec (previously per-channel only via kofin-force-transcode)
- Fix fMP4 container crash on in-progress recordings: profile always requests TS, PostProcessTranscodingUrl switches to fMP4 only for actual AV1 video transcodes
- Preferred video codec included in DirectPlayProfiles — setting a preferred codec implies the device can decode it
- DTS added to audio codec options
- Session metadata moved from Kodi settings to file — eliminates "Unknown setting type" log spam for list[string] codec settings
- Release build matrix now runs on pull requests for cross-platform compile verification
- Fix doubled CI checks on PRs

v0.6.8
- Replace per-codec force-transcode toggles with multi-select Direct play video/audio codec lists
- Add MP2 to the audio codec list (common in DVB broadcasts)
- Add Maximum audio channels setting (Stereo/5.1/7.1) — was hardcoded to 6
- Update settings descriptions: bitrate limit disables direct play, catchup requires unlimited
- Recording playback uses full device profile: respects codec lists, Hi10P/HEVC RExt constraints, subtitle profiles, bitrate limit, and preferred codec
- Completed recordings can direct play when force remux is off and bitrate is unlimited
- In-progress recordings respect kofin-force-transcode and kofin-bitrate-limit KofinProps from source channel

v0.6.7
- Fix v0.6.6 regression where force-remux caused force-transcode: only cap VideoBitrate at source bitrate for actual transcode sessions, not remux — Jellyfin's CanStreamCopyVideo re-checks the URL's VideoBitrate on each master.m3u8 request and denies copy when it's below the source bitrate

v0.6.6
- Fix 4K HEVC-to-H264 transcode failing with "Codec id 27 require extradata" in inputstream.adaptive at unlimited bitrate: cap HLS VideoBitrate at the source stream bitrate (~30 Mbps) instead of the server's ~1 Gbps sentinel so segments stay small enough for adaptive's codec-init window

v0.6.5
- Set MinSegments=3 in the Jellyfin device profile when using inputstream.adaptive so the initial variant playlist comes back populated with multiple segments. Adaptive rejects single-segment playlists with "Codec id NN require extradata" even when the master has CODECS attributes; recordings always worked because they came back populated. Adds ~10s of initial buffering for adaptive users; ffmpegdirect stays at MinSegments=1

v0.6.4
- Fix inputstream.adaptive playback of Jellyfin transcoded/remuxed live TV: keep TS container (not fMP4) and use master.m3u8 — adaptive constructs codec extradata from the master playlist's #EXT-X-STREAM-INF CODECS attribute and demuxes the TS segments cleanly. fMP4 with #EXT-X-MAP confused adaptive's parser; the recording playback path (which always worked with adaptive) was the reference

v0.6.3
- Fall back to the global InputStream setting when the channel doesn't pin one via M3U, so the device profile container (TS vs fMP4) and URL endpoint (live.m3u8 vs master.m3u8) match the inputstream Kodi actually uses
- Update InputStream setting help text to warn that inputstream.adaptive doesn't currently work with Jellyfin's live transcode output; recommend FFmpeg Direct for transcoded/remuxed channels

v0.6.2
- Keep Jellyfin's master.m3u8 path (instead of rewriting to live.m3u8) when the channel uses inputstream.adaptive — adaptive needs the master playlist's #EXT-X-STREAM-INF CODECS attribute to construct codec extradata before opening fMP4 segments

v0.6.1
- Use fMP4 container in the Jellyfin device profile when the channel uses inputstream.adaptive (newer adaptive can't extract codec extradata from MPEG-TS HLS segments)
- Trim whitespace on M3U prop values so trailing spaces don't break boolean parsing
- Read inputstream override from channel properties instead of cached name (was set before KodiProps were applied)
- Re-declare legacy catchupM3U* settings as hidden so migration reads stop spamming "is not a integer" errors

v0.6.0
- Reference playlist promoted to first-class M3U layer (channel groups, KodiProps, KofinProps); catchup is now one consumer of it
- Channel groups from M3U group-title= and #EXTGRP: are added alongside the "Jellyfin" all-channels group
- KodiProps (#KODIPROP, #EXTVLCOPT, #EXTVLCOPT--) honored: per-channel inputstream selection, mimetype, http-user-agent/referrer/reconnect
- KofinProps for per-channel device-profile overrides: kofin-force-remux, kofin-bitrate-limit, kofin-force-transcode (e.g. force HDR transcode on a single channel)
- New global force-transcode codec toggles: AV1, HEVC, MPEG-2, VC-1, VP9 (excludes the codec from direct play / remux)
- Settings reorganised: Playback split into Transcoding + InputStream; reference-playlist path moved to Advanced. Existing M3U paths migrated automatically.

v0.5.4
- Fix recording Premiered date for completed recordings (fall back to DateCreated)
- Set full recording date for skin Premiered info (not just year)
- Default new timer type to manual recording instead of EPG

v0.5.2
- Fix regression in v0.5.1 that broke creating recordings from the EPG (restore "Record once (EPG)" timer type)

v0.5.1
- Fix recording playback being incorrectly marked watched after seek/skip then stop
- Report real player position (not wall-clock elapsed) for recordings
- Drop broken "Record once (EPG)" timer type; manual is now the default

v0.5.0
- Add Windows x86_64 release builds (Kodi 21 Omega, Kodi 22 Piers)
- Drop stale Azure Pipelines config (CI consolidated in GitHub Actions)

v0.4.0
- Rewrite playback reporting as Python service (fixes Kodi hang on stop)
- Dashboard session persists for live TV, recordings, and in-progress recordings
- Report hostname/IP in device name instead of just "Kodi"
- Guard Python reporter against replaying stale sessions on non-kofin playback
- Rename "Input stream" to "InputStream" in settings

v0.3.1
- Fix Jellyfin dashboard session dropping after 5 minutes during live TV playback
- Rename "Input stream" to "InputStream" in settings

v0.3.0
- Add manual timer support (record by channel + time, no EPG required)
- Disable timer editing with error notification (Jellyfin silently ignores updates)
- Fix instant record start time (was showing 01:00 instead of current time)
- Fix manual timer date when only time is edited in dialog

v0.2.5
- Rewrite recording playback state sync using Jellyfin Played flag and PlaybackPositionTicks only
- Fix mark as watched/unwatched syncing to Jellyfin server
- Fix resume position flickering on cursor hover
- Playback start no longer marks recording as watched on Jellyfin

v0.2.4
- Fix watched state sync: derive Kodi PlayCount from Jellyfin Played flag
- Mark as watched/unwatched now correctly updates Jellyfin server
- Fix UI hang when stopping a recording (async timer delete)
- Stop per-recording polling of resume position (reduces network traffic and UI lag)
- Clean up translations: remove old pvr.iptvsimple strings
- Replace changelog with Kofin release history
- Release workflow now includes changelog in release notes

v0.2.3
- Remove duplicate group headings in settings
- Add CI/CD: build script, compile checks, multi-platform release workflow
- Support 10 platform builds: linux (x86_64, armv7, aarch64), android (armv7, aarch64) x Kodi 21/22

v0.2.2
- Migrate settings to new format with descriptions/tooltips
- Change provider to Kontell
- Add in-progress recording input stream setting (adaptive vs kodi internal)

v0.2.1
- Clean up build system to match Kodi project practice
- Fix cross-compilation find_package(Kodi)
- Add setting descriptions, renumber catchup IDs

v0.2.0
- Add catchup/archive support via reference M3U and ffmpegdirect catchup mode
- Catchup parity with pvr.iptvsimple: CatchupController, M3U parser, catchup settings
- Add connection check settings

v0.1.0
- Jellyfin API integration: channels, EPG, recordings, timers
- Account login flow (username/password and Quick Connect)
- Live TV playback with remux/transcode device profiles
- Recording playback via inputstream.adaptive (HLS remux)
- In-progress recording support with timer cross-reference
- Playback stop detection via Python service
- Session reporting for Jellyfin dashboard
- Watched state sync (play count, resume position)
- inputstream.ffmpegdirect timeshift support

v0.0.1
- Fork pvr.iptvsimple as pvr.kofin
- Strip M3U/XMLTV/catchup/VOD data sources
- Replace with Jellyfin REST API calls
